

// export function getResource(modelName: string) {
//   const model = dmm.datamodel.models.find((m) => m.name === modelName)

//   if (!model) {
//     throw new Error(`Model "${modelName}" not found in DMMF`)
//   }

//   return model
// }